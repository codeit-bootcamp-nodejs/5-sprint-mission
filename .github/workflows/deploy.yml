# Github Secrets
# AWS_ACCESS_KEY_ID(duplicated in .env.prod)
# AWS_SECRET_ACCESS_KEY(duplicated in .env.prod)
# AWS_REGION(duplicated in .env.prod)
# ECR_SERVER_URI
# EC2_HOST
# EC2_USERNAME
# EC2_SSH_PRIVATE_KEY
# ENV

name: ci/cd

on:
  pull_request:
    branches: ["sprint-mission-11"]
  push:
    branches: ["sprint-mission-11"]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: checkout branch
        uses: actions/checkout@v3

      - name: install node.js
        uses: actions/setup-node@v3
        with:
          node-version: "lts/*"
          cache: "npm"

      - name: install dependencies
        run: npm ci

      - name: check types
        run: npm run type:check

      - name: check format
        run: npm run format:check

      # todo: check lint

      - name: run unit test
        run: npm run test:service
  cd:
    needs: ci
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: configure aws credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{secrets.ACCESS_KEY_ID}}
          aws-secret-access-key: ${{secrets.SECRET_ACCESS_KEY}}
          aws-region: ${{secrets.REGION}}

      - name: login to ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: checkout branch
        uses: actions/checkout@v3

      - name: build and push docker image to ecr
        run: |
          docker build -t ${{secrets.ECR_URI}}:latest -t ${{secrets.ECR_URI}}:${{github.sha}} .
          docker push ${{secrets.ECR_URI}}:latest
          docker push ${{secrets.ECR_URI}}:${{github.sha}}

      - name: create .env.prod on ec2
        uses: appleboy/ssh-action@master
        with:
          host: ${{secrets.EC2_HOST}}
          username: ${{secrets.EC2_USERNAME}}
          key: ${{secrets.EC2_SSH_PRIVATE_KEY}}
          script: |
            echo "${{secrets.ENV}}" > .env.prod

      - name: copy docker-compose.yml to ec2
        uses: appleboy/scp-action@master
        with:
          host: ${{secrets.EC2_HOST}}
          username: ${{secrets.EC2_USERNAME}}
          key: ${{secrets.EC2_SSH_PRIVATE_KEY}}
          source: "docker-compose.yml"
          target: "/home/${{secrets.EC2_USERNAME}}"

      - name: run docker-compose.yml on ec2
        uses: appleboy/ssh-action@master
        with:
          host: ${{secrets.EC2_HOST}}
          username: ${{secrets.EC2_USERNAME}}
          key: ${{secrets.EC2_SSH_PRIVATE_KEY}}
          script: |
            export SERVER_IMAGE_NAME=${{secrets.ECR_URI}}:latest
            aws ecr get-login-password --region ${{secrets.REGION}} | docker login --username AWS --password-stdin ${{secrets.ECR_URI}}
            docker-compose -f docker-compose.yml pull
            docker-compose -f docker-compose.yml up -d
            docker image prune -f
            docker logout ${{secrets.ECR_URI}}
            rm -f .env.prod
